@model WebApplication1.Models.Coffee
    <div><p>&nbsp;</p></div>
    <h3 style="color: #FFFFCC;">Are you sure you want to delete this?</h3>
    <img style="width:250px; height:250px;" src="@Url.Action("GetCoffeeImage", "Coffee", new { CoffeeID = Model.CoffeeID })" />
    <div><p>&nbsp;</p></div>
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="coffee-links">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }